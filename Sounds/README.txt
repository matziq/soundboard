# Drop audio files here, then run `python build_manifest.py` from the
# parent folder to regenerate sounds.json.
#
# Filename convention:
#   Person-Sound_Name.mp3   → group "Person",  label "Sound Name"
#   Effect-Name.mp3         → group "Effect",  label "Name"
